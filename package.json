{
  "name": "rejuve-gamification-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Re.juve Gamification & Onboarding Platform Monorepo",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "build:frontend": "npm --prefix frontend run build",
    "generate": "npm --prefix frontend run generate",
    "deploy": "npm --prefix frontend run deploy",
    "test": "npm --prefix frontend run test",
    "test:frontend": "npm --prefix frontend run test",
    "audit": "npm --prefix frontend run audit",
    "audit:frontend": "npm --prefix frontend run audit"
  }
}
